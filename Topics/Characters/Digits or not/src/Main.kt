fun main() {
    var isDigit: Boolean
    val a = readln().first()
    val b = readln().first()
    val c = readln().first()
    val d = readln().first()

    isDigit = a.isDigit()
    println(isDigit)
    isDigit = b.isDigit()
    println(isDigit)
    isDigit = c.isDigit()
    println(isDigit)
    isDigit = d.isDigit()
    println(isDigit)
}